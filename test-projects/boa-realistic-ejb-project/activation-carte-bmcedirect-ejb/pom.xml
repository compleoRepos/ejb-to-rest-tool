<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>ma.eai.idev</groupId>
        <artifactId>eai-parent</artifactId>
        <version>2.1.0</version>
    </parent>

    <groupId>ma.eai.boa.xbanking</groupId>
    <artifactId>activation-carte-bmcedirect-ejb</artifactId>
    <version>3.2.1-SNAPSHOT</version>
    <packaging>ejb</packaging>
    <name>BMCE Direct - Module Carte EJB</name>
    <description>
        Module EJB du projet BMCE Direct Digital Banking.
        Gestion des cartes bancaires : activation, blocage, reception.
        Integre avec le core banking via Magix (CART01-CART05).
    </description>

    <properties>
        <java.version>11</java.version>
        <maven.compiler.source>11</maven.compiler.source>
        <maven.compiler.target>11</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <dependencies>
        <!-- Framework EAI Bank of Africa -->
        <dependency>
            <groupId>ma.eai.midw</groupId>
            <artifactId>eai-commons</artifactId>
            <version>2.1.0</version>
        </dependency>
        <dependency>
            <groupId>ma.eai.midw</groupId>
            <artifactId>eai-usecases</artifactId>
            <version>2.1.0</version>
        </dependency>
        <dependency>
            <groupId>ma.eai.midw</groupId>
            <artifactId>eai-services</artifactId>
            <version>2.1.0</version>
        </dependency>
        <dependency>
            <groupId>ma.eai.midw</groupId>
            <artifactId>eai-log</artifactId>
            <version>2.1.0</version>
        </dependency>

        <!-- Java EE API -->
        <dependency>
            <groupId>javax</groupId>
            <artifactId>javaee-api</artifactId>
            <version>8.0.1</version>
            <scope>provided</scope>
        </dependency>

        <!-- JAXB -->
        <dependency>
            <groupId>javax.xml.bind</groupId>
            <artifactId>jaxb-api</artifactId>
            <version>2.3.1</version>
        </dependency>

        <!-- Bean Validation -->
        <dependency>
            <groupId>javax.validation</groupId>
            <artifactId>validation-api</artifactId>
            <version>2.0.1.Final</version>
        </dependency>

        <!-- SLF4J -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <version>1.7.36</version>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>4.13.2</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-ejb-plugin</artifactId>
                <version>3.2.1</version>
                <configuration>
                    <ejbVersion>3.2</ejbVersion>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
