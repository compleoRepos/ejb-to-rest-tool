# ═══════════════════════════════════════════════════════════════
# Compleo EJB Client Modernizer — Template de configuration
# Copier ce fichier vers .env et remplir les valeurs obligatoires
# Usage : cp docker/env-template.conf .env && nano .env
# ═══════════════════════════════════════════════════════════════

# ── Application ────────────────────────────────────────────────
COMPLEO_PORT=8080
NODE_ENV=production
LOG_LEVEL=info

# ── Sécurité ──────────────────────────────────────────────────
# Secret JWT (OBLIGATOIRE — générer avec: openssl rand -hex 32)
SESSION_SECRET=CHANGE_ME_GENERATE_WITH_OPENSSL_RAND_HEX_32
AUTH_STRATEGY=local
LOCAL_ADMIN_USER=admin
LOCAL_ADMIN_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# ── Base de données MySQL 8.0 ─────────────────────────────────
DB_NAME=compleo
DB_USER=compleo
DB_PASSWORD=CHANGE_ME_DB_PASSWORD
DB_ROOT_PASSWORD=CHANGE_ME_ROOT_PASSWORD
# DATABASE_URL est construite automatiquement par docker-compose
# Pour le dev local (pnpm dev) :
DATABASE_URL=mysql://compleo:CHANGE_ME_DB_PASSWORD@localhost:3306/compleo

# ── Stockage MinIO (S3-compatible) ─────────────────────────────
MINIO_ROOT_USER=compleo
MINIO_ROOT_PASSWORD=CHANGE_ME_MINIO_PASSWORD
MINIO_BUCKET=compleo-data

# ── ML / IA (optionnel) ───────────────────────────────────────
OLLAMA_URL=http://localhost:11434
CHROMA_URL=http://localhost:8001
ML_MODEL=qwen2.5:1.5b

# ── Analytics (optionnel — laisser vide si non utilisé) ────────
# VITE_ANALYTICS_ENDPOINT=
# VITE_ANALYTICS_WEBSITE_ID=

# ── SSO / OIDC (optionnel, si AUTH_STRATEGY=oidc) ─────────────
# OIDC_ISSUER_URL=https://sso.example.com/realms/compleo
# OIDC_CLIENT_ID=compleo-app
# OIDC_CLIENT_SECRET=
# OIDC_REDIRECT_URI=http://localhost:8080/api/auth/oidc/callback

# ── LDAP (optionnel, si AUTH_STRATEGY=ldap) ────────────────────
# LDAP_URL=ldap://ldap.example.com:389
# LDAP_BIND_DN=cn=admin,dc=example,dc=com
# LDAP_BIND_PASSWORD=
# LDAP_SEARCH_BASE=ou=users,dc=example,dc=com
# LDAP_SEARCH_FILTER=(uid={{username}})
